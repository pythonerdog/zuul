- tenant:
    name: tenant-one
    source:
      gerrit:
        config-projects:
          - common-config
        untrusted-projects:
          - org/project:
              always-dynamic-branches:
                - "^feature/.*"
