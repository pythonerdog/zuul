- project:
    name: org/project
    check:
      jobs:
        - single-inventory
        - single-inventory-list
        - executor-only-inventory
        - group-inventory
        - hostvars-inventory
        - ansible-version8-inventory
        - ansible-version9-inventory
