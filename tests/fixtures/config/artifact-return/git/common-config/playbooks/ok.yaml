- hosts: localhost
  tasks:
    - zuul_return:
        data:
          zuul:
            artifacts:
              - name: image
                url: something
                metadata:
                  type: test_data
