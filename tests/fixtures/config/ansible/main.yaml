- tenant:
    name: tenant-one
    default-ansible-version: SETME
    source:
      gerrit:
        config-projects:
          - common-config
        untrusted-projects:
          - org/project
          - bare-role
          - org/ansible
